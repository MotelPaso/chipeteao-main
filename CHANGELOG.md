# Changelog

- Iteration 1: Player controller (movement, jump, slide, third-person camera, test arena)
- Iteration 2: Auto-attack weapon system (weapon base class, Shortsword melee arc, health/damage system, target dummies)
- Iteration 3: Enemy spawner + grunt AI (horde seek with separation, melee damage, time-ramped spawns, player health)
- Iteration 4: XP/leveling loop (gem drops with magnet pickup, RunState autoload, pause-and-pick 3-card upgrade UI, data-driven upgrade pool)
- Iteration 5: HUD (HP/XP bars, level, MM:SS run timer, kill counter, low-HP color shift)
- Iteration 6: Run-end flow (death + 15-min survival victory, stats screen, retry/quit, player death state)
- Iteration 7: Hollow Woods arena (low-poly trees/rocks/ramps/platforms, forest mood lighting and fog, perimeter treatment)
- Iteration 8: Weapon variety (Dart Pistol projectiles, Ember Wand AoE bursts, new-weapon acquisition cards, per-owned-weapon upgrades, 4-weapon cap)
- Iteration 9: Tomes + stats layer (8 stackable tomes, global multipliers, crit/lifesteal/armor/luck, luck-tilted rarity)
- Iteration 10: Enemy variety (skirmisher ranged AI, armored tank, glowing elites with 5x XP, time-phased spawn composition)
- Iteration 11: Rotking boss fight (telegraphed moveset, threshold summons, boss HP bar, minute-5 and minute-11 Elder spawns)
- Iteration 12: Shrines & chests (channel-under-pressure Charge, HP-gamble Greed, boss-curse stacks, verticality-payoff chests, interact system)
- Iteration 13: Characters (Rook/Vex/Ash with per-level passives, character select screen, starting-weapon swap, change-character flow)
- Iteration 14: Weapons & roster wave 2 (piercing bow, slowing whip, out-and-back boomerang; Juno speed-to-damage, Bramble thorns, Otto HP growth)
- Iteration 15: Roster complete (rapid twin daggers, cluster-seeking blood pools, evasion + dodge-execute Nyx, lifesteal Doc, Tome of Mist)
- Iteration 16: Meta-progression (shards + save file, quest catalog with claims, character unlock gating on the select screen, run-end earnings summary)
- Iteration 17: Game feel (camera shake + hit-stop + hit flash, death particle bursts, damage vignette, kill-streak popups, level-up pulse, slide FOV kick)
- Iteration 18: Audio (14 synthesized original SFX + generator script, pooled Sfx autoload with pitch jitter and rate limiting, forest ambient bed)
- Iteration 19: Ash Dunes biome (desert arena + props + mood, map select with victory-gated unlock, per-map run/victory counters, dunes quests)
- Iteration 20: Desert roster (line-of-sight laser Sunspitter, burrowing ambusher, per-map spawn phases, Sarcognath boss with rotating beam/coffin ring/entomb root)
- Iteration 21: Secrets (Odd Stump and Humming Skull triggers, Grubthing and Coffer Mimic minibosses, boss-kill character unlocks with select-screen hints)
- Iteration 22: Pause & settings (Esc pause menu, SFX/ambient volume buses, mouse sensitivity, fullscreen toggle, persisted in save)
- Iteration 23: Map tiers (T1-T3 per map with scaled enemies/bosses/XP and shard bonuses, win-gated unlocks, tier picker + HUD tag, tier quests)
- Iteration 24: Performance (NodePool + Pools autoload for the five churn hotspots, staggered separation, hidden perf overlay, zero steady-state allocation)
- Iteration 25: QoL (screen-edge boss arrow, level-up gem vacuum, elite/boss health orbs with cap, low-HP bar pulse)
- Iteration 26: Containment & balance (perimeter walls + void fail-safe, aim flatten guards, tuned spawn ramp/weapon values validated by soaks)
- Iteration 27: Export & build (macOS/Windows/Linux presets, v0.1.0 stamp, verified exported macOS build boots clean)
- Iteration 28: Handoff docs (Spanish README with run/build/verify instructions, ARQUITECTURA.md extension map for every system)
- Iteration 29: UI overhaul (design system theme, animated character select and upgrade cards, restyled HUD bars/badges, screen transitions, menu polish)
- Iteration 30: Seal & VFX (foca model with per-character tint and procedural motion, invisible held weapons, effect-first combat visuals per weapon)
- Iteration 31: Bigger maps (160x160 arenas, denser rims, extra shrines/chests/verticality spots, centralized bounds, travel-speed compensation)
- Iteration 32: Local co-op (1-4 player lobby with per-slot characters, device-isolated input via Coop autoload, gamepad support in solo, split-screen viewports, downed/revive system, round-robin upgrade picks, nearest-player AI/pickup targeting, party HUD, all-down defeat)
- Iteration 33: Difficulty & living map (harder spawn ramp and elite chances, open-ended late-run stat ramp, co-op party scaling, per-run randomized prop scatter and POI layout shuffle, WorldDirector timed events: beaconed supply chests, elite hunt packs, essence rifts)
- Iteration 34: World backdrop & camera modes (per-biome distant ground disc + fog-blended horizon hill rings; per-player camera cycling C/right-stick-click: third person, first person with own-body culling via visual layers, free-orbit camera with wheel/D-pad zoom and movement-facing body)
- Iteration 35: Build depth (weapon evolutions via invested-weapon + paired-tome + chest ceremony with fanfare/banner, EvolutionCatalog recipes for all 10 weapons, per-weapon upgrade-level tracking, Collection screen with raiders/arsenal/evolutions/bestiary and completion %, bestiary kill counters, evolution quests)
- Iteration 36: Sticky meta (Armory relic shop — 6 permanent stat relics bought with Shards on a rank/price ladder, applied in PlayerStats recompute; seeded Daily Hunt with deterministic character/map/layout/cards and per-date best score; Endless mode — continue past a victory with no clock, delta-only fold, best-endless high-water; quests for all three)
- Iteration 37: Content wave 3 (Gloomfen marsh biome unlocked by an Ash Dunes win, MARSH spawn phases, Fenwraith boss — miasma nova / bog volley / sink-and-rise blink; Spirit Orbs orbiting-contact weapon and Storm Rod chain-lightning weapon with cards + evolutions; Wisp and Torren raiders; Gloomfen quests)
- Iteration 38: Loop rules (no run clock — 15-min survival grades the run and Extract from the pause menu ends it; Endless mode removed; weapon evolutions by weapon level 6 instead of chests; two-stat weapon cards; 5-weapon and 5-distinct-tome caps; Stout Heart heals only the gained HP; level-ups no longer vacuum gems; Spirit Orbs orbit independent of body rotation)
- Iteration 39: Stat layer 2 (six new tomes — Fortune/luck, Multitude/+projectiles, Lingering/effect duration, Wisdom/XP gain, Chance/gambling boons rolled per rarity, Peril/run-wide difficulty; WeaponBase projectile and duration helpers wired into pistol, bow, orbs, whip and blood pools; XP multiplier applied at pickup; RunState difficulty sources scale spawns, bosses and XP)
- Iteration 40: Run economy & items (per-player run points paid by kills — elites x5, bosses 25; ItemCatalog with fixed-rarity items and unlimited copies held in a per-player ItemBag; chests now cost points by rarity with a global x1.25 price growth per open and pay out one item of their rarity, rarity-tinted; luck tilts chest rarity; enemy poison with Fart Bag / spider projectiles from the Superhero Mask, Magnet timed vacuum, Titan Blood body scale, Demon Blood / Master Key / Cosmic Worm counters for later systems; HUD points badge and bottom-left loadout strip of weapons, tomes and items with placeholder glyph tiles and an `icon` hook for real sprites)
- Iteration 41: Altars (charge altars fill by proximity with spawn surges, forfeit and sink when you leave, time out unclaimed, and grant party-wide flat stat boons independent of cards — Master Keys speed and enlarge them; the Curse obelisk becomes the demonic altar: same ritual plus permanent run-wide difficulty per use, bigger boons, Demon Blood scaling, more elites and one extra boss chest per use; bosses drop Rare+ chests; roulette wheel altar with a fixed-price spin menu and eleven outcomes from Legendary items to curses and enemy frenzies; paired portals placed at run start with shared cooldowns shortened by Cosmic Worms; springs that heal and grant a 30 s power-up for a fixed price; WorldDirector spawns altars and springs as recurring timed events; old boss-curse banking removed, HUD shows the difficulty bonus)
- Iteration 42: Living threat (timed hordes at minutes 3/7/10/13 then every 3 min, in waves sized by minute, difficulty and party; bosses return every 4 min after the Elder, each 25% stronger; late-ramp XP scaling so tougher spawns pay more; elites drop chests with difficulty-tilted rarity; sky events — Blood Moon berserkers and Eclipse shades with tinted sun and ambient light, always followed by a Full Moon that buffs XP and luck; demonic altar uses raise sky-event odds; enemies climb walls and props and stay clamped inside the arena)
- Iteration 43: Arsenal wave 4 & companions (Slime Trail puddles along your path, Aura pulse ring, Stench gas pulses that poison and slow, Kamehameha lane beam with shake and hit-stop — each with cards and an evolution; Miro, Bogg and Kael raiders; PetCatalog companions — Alien, Dinosaur, Angry Bird, Capybara — that follow the raider, carry their own scaled weapon outside the 5-weapon cap and grant a stat per run level, obtained as pet items from chests and the roulette, extra copies feeding the pet weapon)
- Iteration 44: Irregular arenas (per-run cell mask over the 160x160 floor — random rock-blob regions blocked with tall colliders and oversized rocks, flood fill from spawn seals unreachable pockets so every open cell is walkable on foot, spawn rings, surges and POI placement all consult is_walkable; scene-placed chests and altars randomly sit a run out and 0-2 extra chests appear; Daily Hunt seed drives the mask too)
- Iteration 45: Localization & hardening (full Latin American Spanish pass over every player-facing string with docs/GLOSARIO.md as the canonical term list, English kept for ids/node names/groups/StringNames/save keys/soak logs; 252 bug fixes across player, weapons, enemies, spawner, world, catalogs, UI and infra — runtime-mutated scene sub-resources marked resource_local_to_scene, party-wide payouts now reaching downed raiders, cameras injected instead of looked up so co-op split-screen gets shake/FOV/boss arrow, party-aware boss AoE, transactional save counters so an abandoned run persists nothing, atomic save writes with backup recovery; 96 refactors extracting shared chassis — CatalogIndex, MagnetPickup, MetaScreen, CardFactory, FxMesh, WeaponBase area sweeps, one POOLS table; test harness rewritten so the probe walks, jumps and interacts through the real input actions, plus tools/verificar.sh gating import + three-arena soaks on coverage)
- Iteration 46: Progression rules (tome stacks and weapon levels uncapped — one roman-numeral helper feeds both the card title and the HUD corner; weapons evolve at level 10 and ASCEND at every further multiple of 10 with a flat recipe-free step, so weapons without an evolution keep improving; convex XP curve, level 10 costs 50 instead of 35 and level 20 costs 125 instead of 65; Tome of Multitude now reaches all 14 weapons — extra attacks staggered and fanned for single-hit weapons, wider/faster fields for Aura and Stench, more puddles for Slime Trail; each level-up offers ONE side of the pool, weapons or tomes, named in the title; every roulette spin doubles the price for the rest of the run; «enfriamiento» renamed to «velocidad de ataque» as a positive bonus across cards, tomes, relics, passives and altars)
- Iteration 47: Economy, chests and altars (elites are now SHINY — the flat amber tint becomes a per-body hue-cycling emissive shimmer with a brightness pulse, material only, no particles; free chests that cost nothing, do not inflate the paid ones and roll their rarity WHEN OPENED with the opener's luck — every boss and shiny drop, the demonic pact chest and ~15% of the run-start chests, with boss chests losing their Rare floor; every chest sinks and frees itself after paying out; altars never time out and never forfeit — leaving the ring only pauses the charge, the ring is three times wider and brightens and pulses as you approach, spawn surges are gone, and every altar sinks once spent; the WorldDirector seeds altars through the event tick with a gap compressing toward 20 s and a cap on unspent altars that grows by the minute, beacons burning until the altar is used; completing a charge altar opens a choice of three boons on the card picker instead of rolling one, and the demonic altar offers three PACTS, each a benefit — bigger boon, run points or a free chest — paid for with run difficulty, shiny odds, sky-event length or frequency, or the disasters of a later wave; HUD loot toast fed by ItemBag for every source, and Tomo del Azar finally shows what it rolled)
- Iteration 48: Bugs, feel, HUD and settings (nothing spawns on top of a raider — min_player_clearance gates every spawn path through one funnel, the boss included, and dropped spawns are counted and logged once a minute; the invisible 10x7x10 mask walls are gone, blocked cells are filled with real rocks whose every visible blob now has a baked convex collider, which also ends the "enemies flying" report — climbers were walking on top of those 7 m boxes at y 7.0-7.5; the soak harness gains a permanent mask sweep that walks every blocked-cell boundary with a player-sized capsule, a raider-inside-blocked-cell watchdog, an airborne detector and a BONK_ELITE_BOOST switch that makes every spawn shiny; make_elite and boss tier scaling grow the visual rig and resize a per-instance collision shape instead of scaling the CharacterBody3D, which Godot does not support; spawn ramp raised ~25-35% from minute 3 on, leaving the opening minute intact; optional FPS badge persisted in the save and toggled in Ajustes; the loadout strip splits into weapons+tomes bottom-left and items bottom-right, every slot drawing res://assets/icons/<library>/<id>.png with a committed placeholder fallback; multi-jump and power-up drop chance stats, both sold by altars, with a Rare "Botas de resorte" item)
- Iteration 49: Persistent run root and swappable arenas (Run.tscn owns the run — Player, RunManager, WorldDirector, HUD and the card picker now survive a map change, and the biome is instanced under ArenaHost and thrown away per stage; the three arena scenes drop their RunSystems instance for an arena.gd root with a SpawnPoint marker, and sequence the stage lifecycle explicitly: mask, director handshake, then scatter.place_props() so keepouts still see the shuffled POIs; RunState grows stage_index/lap/stage_time/stage_cleared/pseudo_infinite_time and a stage_changed signal; ScreenFade.transition_async awaits its action between the fades, which transition() cannot; the stage swap frees every enemy, reclaims every pooled node through Pools.release_all_live, frees the stage's chests, altars, portals, beacons and gems, prints a Stage sweep: line that must be all zeros and a Stage carry: pair that must match, then builds the next map and places the party; every world spawn that used get_tree().current_scene now parents to the arena root through RunRoot.stage_parent, so nothing from one stage leaks into the next; an exit portal opens when the stage clears and the boss arrow points at it while no boss lives; BONK_STAGE_FAST and BONK_SAVE_PATH join the harness switches and verificar.sh gains a fourth, stage-crossing soak)
- Iteration 50: Stage flow, pseudo-infinite mode and the end of map tiers (a stage is cleared when its own clock passes 15 minutes AND its Elder is dead — the spawner keeps that one instance and logs Stage boss slain:, and an arena with no boss counts as clear; clearing opens the exit portal, starts the pseudo-infinite ramp and grades the run, so "victory" now means "cleared at least one stage"; the boss schedule and the hordes key off RunState.stage_time so every map gets its own boss at minute 5 and Elder at 11, while the global ramps keep reading the run clock; each completed lap of the map list multiplies fresh spawns and bosses, replacing the map tiers; while a stage is cleared the map answers per minute — HP +15%, damage +10%, speed +4% capped at x1.8, spawn interval x0.85 compounding, hordes x1.5 — and logs Pseudo-infinite: once a minute; the map picker, the G1/G2/G3 tiers, the map unlock gates and every tier symbol are gone from the select screen, MapCatalog, SaveData, GameConfig and the spawner, the HUD badge becomes E%d · V%d, and GLOSARIO rule 6 retires «Grado» for «Etapa» and «Vuelta»; fold_run_results takes the maps a run visited and cleared so per-map quests keep working across a multi-map run, the tier quests keep their ids as lap quests and the endless quests count pseudo-infinite minutes; the run-end screen reports stages and laps instead of a tier bonus)
- Iteration 51: 240x240 arenas with terrain relief (a Terrain node replaces the flat floor box in every biome — a low-amplitude heightmap seeded from the scatter's own per-run seed, so relief and props always agree, with flat pads under the spawn, the authored verticality groups and the new mesas, a flat rim band for the static perimeter walls and the backdrop, a HeightMapShape3D collider at one sample per metre and a packed-array render mesh that builds in ~100 ms; the biome floor shader moves onto it unchanged; nothing in the project is placed at y = 0 any more — scatter props, mask rocks, the perimeter line, director points, events, portals, roulettes, altars, springs, the exit portal, the shuffled POIs, the party spawn and its co-op ring, boss death chests and enemy spawns all go through Terrain.height_at, and both ground probes now span the whole relief with height_at as their fallback instead of zero; the scatter picks mesa sites before the terrain builds so each gets a flat pad, then raises a platform and ramp on it; arenas grow to 240x240 with prop counts, mask blobs and shuffled POIs scaled x2.25 and statically authored walls; BONK_GAME_SEED makes a whole run reproducible; the probe's waypoints, arrival checks and blocked-cell watchdog work in XZ against the terrain, its mask sweep centres each capsule over the ground and excludes the terrain body, its legs get 40 s and it reports Probe legs:; verificar.sh moves to 360 s per soak)
- Iteration 52: Minimap, full map and fog of war (a north-up minimap sits in the corner of EVERY player view — solo in the top right under the FPS badge, and in co-op anchored inside each split-screen cell through `SplitScreenView.cell_rect`, which stopped being private for exactly this; Tab (BACK on a pad, per-slot through `Coop`) opens that view's own full-screen map on layer 8 with the stage and clock in the header, a legend, and three panels — «Estadísticas» with the whole derived stat layer, «Jugadores» with every raider's HP, level and points including downed ones, and «Objetos» with items, weapon levels and tome stacks; the map itself is ONE helper, `MapDraw`, shared by both consumers so a 150 px widget and a half-screen overlay can never drift — it builds the stage picture once per stage from `Terrain.map_image` (biome tint shaded by height, mask cells darkened), composites it against the fog on a 10 Hz tick and collects markers filtered by what the fog has revealed; `FogOfWar` is a run-scoped L8 image in RunSystems, 2 m per pixel, painted as a disc per live AND downed raider every 0.2 s, shared by the whole party because four raiders in four corners are exploring one map, reset per stage by the WorldDirector and exposing `explored_fraction()` for the soak; regular enemies are NEVER drawn — the horde is what you can already see, the map is for what you cannot — so the marker table has eleven kinds (raiders and the exit are always visible, everything else has to be found) plus five reserved for part C, and a node joins it by setting `marker_kind` on its `Interactable` and nothing else; spent altars and opened chests drop off the map by the `available` flag they already had, and the secret triggers keep an empty `marker_kind` on purpose; every Control in both widgets is FOCUS_NONE because Tab is Godot's own `ui_focus_next`; the probe presses Tab every 60 s, holds it 2 s and prints `Map overlay: open markers=N` / `Map overlay: closed`, its status line ends in `explored=` and a stage that finishes under the coverage gate raises a warning — the gate itself was calibrated once at 0.09, 60% of the worst of three 360 s biome soaks (0.15/0.32/0.16), the margin covering measured run-to-run variance rather than a lazy tour; and the very first thing that gate caught was that `BONK_GAME_SEED` had never actually reproduced a world: the scatter seeded itself with `RandomNumberGenerator.randomize()`, which draws from system entropy and is deaf to the `seed()` `RunState.reset()` had just set, so two soaks with identical seeds built different masks, different props and different relief — it now draws its seed from the run's own stream with `randi()`, which leaves a normal run exactly as random and makes a seeded one reproducible end to end)
- Iteration 53: Temporary power-ups and the reworked spring (a ten-row PowerUpCatalog of loud, brief effects — Celeridad total, Furia, Sabiduría brígida, Fiebre del oro, Reflejo, Modo vampiro, Vuelo, Inmortalidad, Tiempo detenido and the Estrella, which is all nine at once on its own shorter clock and is ~2% of the roll; a row with `effects` needs no code at all because the timed-boon channel already exists, a row with `kind` gets exactly one branch, and different power-ups stack while the same one refreshes — never shortening — which is why every kind-driven consumer is re-synced from has(kind) after any change instead of being switched off at expiry, and why timed boons grew a `tag` so a re-pick replaces its own effects instead of stacking with itself; PowerUps rides on the Player beside ItemBag and owns the toast, the announce and the log, so a new source only has to call apply(); Health gains `invulnerable` (+ a blocked_hits counter the soak reads) and `reflect_fraction`, evaluated BEFORE the invulnerability refusal so immortal-and-reflecting means the horde kills itself, with EnemyBolt now naming its launcher so bolts reflect too; the vampire banks permanent max HP as a stored total re-added by PlayerStats.recompute, never onto a derived value; Fiebre del oro registers a NAMED factor in a product of points sources rather than a second wallet; Vuelo climbs while the jump action is HELD to a ceiling measured above Terrain.height_at, clamps to the arena edge, and on expiry glides a raider stranded over a blocked cell down to the nearest walkable ground — the one thing that would otherwise trip the probe's blocked-cell watch; Tiempo detenido lives on the spawner, which stores and zeroes each body's velocity, freezes what spawns mid-window too, flags pooled enemy bolts instead of toggling their processing (NodePool restores what it snapshotted, so a bolt frozen that way returns inert forever) and makes EnemyBase._on_died restore process_mode before its death tween, because a disabled node still receives the calls that kill it; kills drop a pickup on a 0.4% base finally scaled by A's orphaned powerup_drop_chance stat, x5 on shiny bodies, and the star never drops at all — its only supply is the WorldDirector's star_sighting, which sends it roaming at 6 m/s bouncing off everything unwalkable; the spring now heals and hands one random power-up, never times out, and is capped at one alive through its event row's weight; pickups are stage-scoped, counted by Stage sweep as pickups= with the verificar.sh literal extended to match, and drawn on the map in their own colour; HUD tiles with seconds left above the HP bar, a count on each co-op teammate row, a «Power-ups» section on the Tab map, and three probe switches — BONK_POWERUP_NOW re-granting on every expiry, BONK_STAR_NOW dropping a stationary star, BONK_POWERUP_BOOST making drops visible inside a soak; frozen bodies also go non-solid for the length of the freeze and frozen bolts stop hitting, because "players free" has to mean free to move and a disabled body cannot be pushed — weapons target by group and distance, never by collision layer, so a non-solid frozen enemy stays fully damageable; and two PRE-EXISTING bugs the new verification runs surfaced, both measured on the iteration-52 head first: enemies kept the upward impulse move_and_slide() gives a body depenetrated out of an eighty-strong crush, which tripped the harness's airborne detector in 3 of 3 stage soaks before this fix and 0 of 4 after, and blood_vial.gd bound the flask NODE into its throw tween, so a stage swap freeing the flask mid-flight left Godot unable to marshal a freed Object into the bound argument — it binds the instance id now)
- Iteration 54: One companion slot, vendors and three raiders (pets stop being items — the four `kind: "pet"` rows and the kind itself are gone, so a chest or a roulette can no longer hand one out; the run has ONE slot, `Player.pet_id`, moved only by `set_pet()`, which frees the old companion, spawns the new one, announces both halves of the swap and owns the `Pet joined:` log, so a new source cannot forget to announce itself; copies went with it, which is why every row now declares BOTH a weapon and a stat — a pet is one whole package and a half a row would make a swap an irreversible downgrade — with nine rows carrying nine distinct weapons and nine distinct stats, capybara on the Pararrayos and velocidad de ataque as the team fixed it, alien moved to the Varita de brasas, and Doki, Pony, Cj7, Calabaza mágica and Pokemon added; the pet weapon still rides outside the five-weapon cap and reads the carrier's stats, and still never bumps `used_weapon_<id>` — both bump sites stay unreachable from `Pet._build_weapon`, so the Collection cannot unlock an arsenal entry nobody earned; the pet box is free and one-use, rolling a companion that is not the one you have and ASKING when there already is one, with «Cambiar» as option 0 because the harness always presses the first; three vendor kinds share one table and one rule — the stall owns the economy and the one-sale rule, VendorUi owns the moment — the trafficker selling a choice of three pets at a flat price, the power-up vendor one rolled power-up (never the star) at a run-wide price starting at 100 and multiplying by 1.5 per sale, and the item vendor four luck-rolled items at chest prices that feed the same inflation; the kind is rolled hidden and neither the announce nor the beacon names it, because walking over to find out IS the event; a stall stays until it sells but goes quiet for 20 s after a menu closes empty, since the soak re-fires interact every 0.75 s and would otherwise reopen it forever; two alive at most, through the same weight-zero channel the altars and the spring use; VendorUi follows roulette_ui.gd — layer 15, pauses and always unpauses, closes itself if buyer or stall vanish, and self-plays headless; Boxeador, PNG gucci morty and Backyardigan join the roster free from the start, and a card name now wraps instead of trimming because a proper noun with an ellipsis is a different name; `Stage sweep:` grows `boxes=` and `vendors=` with the verificar.sh literal extended to match, and the probe gains BONK_POI_NOW and BONK_POINTS and now push_errors on an unknown BONK_CHARACTER instead of silently soaking the default raider)
- Iteration 55: Weather — rains, disasters, exclusivity and the event altar (the sky events become ONE channel: `active_weather` holds at most one row of a nine-row WEATHER_CATALOG, so "only one moon, rain or disaster at a time" is a property of the data structure instead of an agreement between three timers; in code it is WEATHER and never "event", because EVENT_LIBRARY is the POI table and the two meanings collided on every read; a row carries id, group, weight, duration, start/tick/stop method names and an optional follow_up — blood moon and eclipse still chain into the full moon, and the two new rains chain into the golden one, which is the reward for having survived the bad one and is never rolled directly; the event tick rolls rarest group first (disaster, then rain, then moon) so a disaster is not crowded out by the commoner weather, the min gap now covers all three groups, and moons and rains stretch with the demonic pact's sky_duration_multiplier while disasters stay fixed — a pact selling longer moons must not also sell a longer meteor shower; disaster_chance_bonus finally has the consumer its comment block promised; `start_sky_event(kind)` stays the public group hook and now FORCES a row, stopping whatever runs (through its own stop, never dropped) and ignoring the cadence gap, which is the one entry the probe, BONK_WEATHER_NOW and the event altar all use, and the moons keep their verbatim `Sky event:` log because verificar.sh's stage gate counts it; enemy rain telegraphs 3-6 ground points every 1.5 s and spawns through a new capped EnemySpawner.spawn_at_points — never dropping bodies from a height, which is exactly what the airborne detector exists to catch; radioactive rain leaves an AcidPool on every corpse plus one every 2 s, a pooled puddle that owns its own damage tick (unlike the cosmetic BloodPoolFx) and burns raiders only, capped at 24 live with 3 m spacing and skipping silently past either; golden rain doubles points on both the player and downed_players groups and halves chest, roulette and spring prices through RunState.price_discount; the tsunami pours one 40+ body wave along a 60 degree arc in per-frame chunks and ends itself, the earthquake shakes and jitters the HUD CanvasLayer offset so every element it owns moves including the minimaps, and the meteor shower marks zones then lands them for triple damage on enemies; the event altar summons any row except the two rewards and refuses rather than queues while weather runs; a HUD badge beside the clock names what is falling and counts it down; the live row's stop now runs from on_stage_ended and _exit_tree, because a golden rain ending with the stage would otherwise leave a x2 points source on every raider and a price discount in RunState)
- Iteration 56: Three items, the necromancer staff and lucky blocks (Cinturón eléctrico fires a bouncing bolt on every dash — the Player grew a `slide_started(direction)` signal because nothing outside it had ever needed to know the dash happened, and the chain starts from `carrier_player().global_position` because ItemBag extends Node and a Node3D child of it would sit at the world origin; the damage goes through a BeltBolt WeaponBase parented to the BAG and not the Weapons mount, so the HUD strip, the five-weapon cap and the level-up pool never see it while crit, lifesteal and the kill hooks come free; Sangre sayayin counts kills and lights a 12 s aura of TAGGED boons so a re-trigger replaces itself instead of stacking, with a golden material_overlay shell the way Juice.flash does it and never SealRig.apply_tint, which is the character's identity colour; Zenkai ARMS on Health.damaged below 10% — the only signal that means 'something hurt me', since take_damage never emits hp_changed — and TRIGGERS on hp_changed above 50%, ignoring anything raised while PlayerStats.is_recomputing(), because _push_bonus_max_hp_to_health writes max_hp and heals from inside recompute and the item would otherwise trigger off its own stat rebuild forever; its stacks are a permanent all-stat channel; the Báculo de nigromante raises whatever it kills — the corpse dies completely first and pays out everything it owes, since Health.died is synchronous and _on_died has already run when deal_damage returns, and the servant is a fresh copy of the same scene, so a kill is a kill AND a servant; a servant pays NOTHING when it expires because it already paid once as a corpse; possession is gated by EnemyBase.possessable and switched by a new `_pick_target()` that _physics_process uses instead of calling Coop.nearest_player directly, so one override changes sides and every consumer below stays target-agnostic — only grunt and tank qualify, with skirmisher, sunspitter, duneburrower, boss_base and secret_boss_base opting out because their reach is wired to the "player" group; a servant leaves the enemies group, which hides it from player weapons, the spawner's live count and time stop in one removal, and takes layer 8 plus an explicit collision exception per raider because players share layer 1 with the world; lucky blocks look like chests and are not — free, rare, always paying one of six weighted rewards, with the well trading one item for the next rarity up and paying a power-up outright when the raider carries nothing, because open_choice returns early on an empty list and a silent block reads as broken; ItemBag.remove_item, Stage sweep grows possessed= with the verificar.sh literal extended, and the probe gains BONK_WEAPON, BONK_ITEM_NOW with a dash cadence, BONK_ZENKAI_TEST and possessed=a/b appended to its frame line)
- Iteration 57: Harness coverage — co-op soak, UI probe, catalog lint and per-run saves (the round-2 work shipped with seven of the nine UI scenes never booted by anything, so a handler that errored on its first press could not fail a soak: `scenes/tests/UiProbe.tscn` boots them and drives the REAL controls through thirteen named steps, with its driver parented to the tree ROOT — deferred, because adding a child to the root from inside a `_ready` trips "Parent node is busy setting up children" — since `%StartButton` and the run-end buttons REPLACE `current_scene` and a driver living inside the scene under test would be freed halfway through its own test; it waits out `ScreenFade`'s busy window after every cut because a press landing there is dropped in silence, resolves every `%` name against its OWN owner because `%SettingsButton` exists in both CharacterSelect and PauseMenu, asserts `not disabled and is_visible_in_tree()` before every `pressed.emit()` (which bypasses both), and drives `%FpsCheck` by `button_pressed` rather than `pressed.emit()` because a CheckButton's handler hangs off `toggled`; its per-step frame deadline is a HANG detector and not a stopwatch, so the two steps that wait on the run clock carry their own longer plazo instead of failing a healthy run; `scenes/tests/CatalogLint.tscn` adds the nine cross-reference checks a soak can never make — a broken cross-reference does not crash, it silently does nothing — and counts `rows` per check because a check that walks an empty list passes for the wrong reason, with `MapDraw.LEGEND_VARIANTS` naming the three legend omissions that are deliberate so the lint can tell them from a forgotten one; the probe learned co-op — `Coop.configure` BEFORE `Run.tscn` is instanced because that is what `RunSystems._spawn_party` reads, godmode scoped to slot 0 so slot 1 stays mortal and `BONK_DOWN_NOW` can land, every "the raider" helper resolving the leader by `player_index == 0` instead of by group order, `_send_action` taking a SLOT because `Player._tick_downed` polls the RESCUER's own action set, a hold mode because a revive wants three continuous seconds and a press released next frame can never produce them, followers that never interact so the interaction counts every gate was calibrated against stay put, and a rescue rule that outranks even the exit rush; a mortal soak now ENDS at the death it was always going to have, through `RunManager.run_ended`, instead of sitting in its own WEDGE warning under a run-end screen that has no headless self-play, and the fog gate gains its third exemption as a SEMANTIC rule rather than a switch — a finished run cannot explore — announced by `Probe fog: skipped` so the skip is never silent and failed by verificar.sh in any godmode soak, because an immortal raider has no business dying; every soak and harness now gets its own `BONK_SAVE_PATH`, deleted with its `.bak` and `.tmp` siblings, since deleting only the `.json` was never a reset — `load_from_disk` falls back to the backup — and the shared file carried each run's `kills_<script>` and `used_weapon_<id>` bumps into the next one; `Player revived:` and `Vendor buyer:` are new lines beside the frozen ones, never fields added to them)
- Iteration 58: Audit batch 1 — the ledger, six stress soaks and thirty-three root fixes (`docs/AUDITORIA-RONDA-2.md` opens with the baseline margins and carries every finding of the six read-only lanes plus the five the stress soaks found; the three HIGH ones ship with an assertion shown failing at `pre-audit`: L1-1, a stage swap landing on an open vendor or roulette left the tree paused FOREVER — those layers hang off the arena, so the swap freed them without their `_close()`, the only place either hands the pause back, and Esc is refused on an already-paused tree — now closed through the `blocking_ui_closable` hook the group was declared for, BEFORE anything reads `was_paused`, and `BONK_SWAP_MENU=1` turns `Swap menu: paused=true` into `Stage advanced: 1 -> 2 (ash_dunes)` + `paused=false`; L4-1, Sangre sayayin tagged its three boons and never called `clear_timed_boons`, so a re-trigger inside the twelve seconds STACKED and the first expiry took back half — `Saiyan boons: max=9 of 3` becomes `max=3 of 3`; L3-1, a burrowed Duneburrower has left the `enemies` group on purpose and the spawner's freeze scanned only that group, so the one enemy that can hurt you through Tiempo detenido was the only one never frozen — fixed with a `burrowed` group, its assertion green at HEAD but WITHOUT the failing pre-audit run the proof rule asks for, and the ledger says so. The stress soaks found five more the lanes could not: the probe's power-up detour outranked the exit rush, so a horde dropping loot every few seconds kept the tour in a 20 m bubble and a 1200 s normal-mode soak never crossed a stage (coverage 0.06 → 0.22); the anti-bubble rule that fixed it then had to learn to yield to the rush as well; the pause menu READ the `ui_blocking` group since it was written but never JOINED it, so its Esc pause was invisible to every consumer; the roulette shrine had the vendor's missing blocking check and no retry cooldown at all; and a Skirmisher shoved onto the raider fires from the exact point it aims at, normalizing a zero vector into a `look_at()` that errored 57 times in one soak. The rest: weather never stopped when a run ENDED, time stop dropped on a stage swap, a servant's tint erased by its first damage flash, a dead servant counted against `max_possessed`, soul bolts still homing on a body another carrier had raised, slime and blood puddles pulsing invisibly at the previous arena's coordinates after a swap through the `clear_weapon_fields()` hook no weapon had ever implemented, the lucky well losing its refusal card because the card UI drops a fourth option in silence, the exit portal spending itself on a swap that was declined, and eight leaks and guards in the UI layer)
- Iteration 59: Audit batch 2 — the drift, the last leaks and the closed ledger (what the second batch fixes is mostly what the first batch could not reach without a soak to confirm it: the full moon handed its two boons to the standing party only, so a raider that happened to be down when it rose came back to a deficit nothing would ever repay — now both groups and TAGGED, since `add_timed_boon` appends and only `clear_timed_boons` drops by tag; the tsunami sized its wave off the RUN clock while iteration 50 had moved the boss timetable and the hordes onto the stage clock and the architecture doc documented the wave the same way, so a tsunami on a fresh third stage asked for a wave sized by half an hour of play; `set_pet` found the outgoing companion BY NAME, and `queue_free` leaves the node a child until the end of the frame, so a second `set_pet` in one frame made `add_child` rename the incoming Pet and no later call could ever find it — two companions following one raider, both firing, forever; the supply chest built its sink tween on the DIRECTOR, which outlives the arena the chest is being freed with; `_spawn_start_pet_boxes` drew a random number even at a chance of zero, which shifts every shuffled POI after it on a shared stream; the run-end screen threw away `leave_run`'s answer that screen_fade asks callers to check. The sweep's `beacons=` field turned out to be decorative — `on_stage_ended` dropped the LIST and left the nodes to die with the arena one step later, so the counter could never be anything but zero; it now frees the nodes, which makes the gate real and the first run with it caught two beacons standing. The rest is drift, and there was a lot: the stage-swap step that claimed to stop the director (it stops itself, one step later), a six-field `Stage sweep:` that the code prints with ten and verificar.sh greps with ten, a HUD hook that has not existed since the tiers went, the spring documented with a boon channel iteration 53 deleted, a group table that still said the closable-menu hook had no caller, README counts of 13 raiders and 14 weapons for 16 and 15, a GLOSARIO whose character table stopped three raiders short and whose item glyphs still listed four pet eggs that no longer exist, and rule 21 written for three map panels when there are five. Closed with the export gate green, five deliberate gate failures shown and reverted, and `docs/AUDITORIA-RONDA-2.md` carrying all 80 findings: 68 fixed, 12 deferred with a reason each, and the one HIGH whose pre-audit proof could not be produced saying so instead of pretending)
- Iteration 60: The harness grew a camera, and the first thing it saw was that the ground was not there (every gate this project has ever run is `--headless`, whose dummy DisplayServer compiles no shader and rasterises no triangle, which leaves a whole class of failure with no possible gate: a mesh wound the wrong way, a material that never binds, a panel drawn over the clock, a label clipped to two letters — all of them produce a perfectly green log, and iterations 51 through 59 passed `verificar.sh` with THE FLOOR OF ALL THREE ARENAS INVISIBLE until the player booted the exported build and said so. `scenes/tests/shot_camera.gd` (`class_name ShotCamera`) is behind `BONK_SHOT_DIR` and carried by BOTH harnesses: it saves PNGs from inside the engine with `Viewport.get_texture().get_image()` AFTER `await RenderingServer.frame_post_draw` — before that await `get_image()` hands back the previous frame, which for an event shot is the frame before the event — and never with a screen grabber, because macOS refuses a terminal's `screencapture` without the Screen Recording permission and a soak has nobody to ask; under `--headless` it says `Shots skipped: headless` once and does nothing, so no gate changes and with the variable unset every line of it is a no-op. It fires every `BONK_SHOT_EVERY` seconds off the PHYSICS delta rather than `RunState.run_time`, precisely so a menu that freezes the run clock is the thing that gets photographed, plus one shot per stage change, weather, vendor arrival, pet box, lucky block, power-up and Tab map — all read off the TREE and not off the log lines those systems print, since a `print` is not something another node can subscribe to: a vendor is a new member of the `vendors` group, the box and the block announce themselves through the `interaction_completed` they already emit, and a power-up is an id the leader did not carry last sample — and one of the first upgrade card, which the probe otherwise answers on the frame it opens. Stage, weather and map shots wait out `SHOT_SETTLE` because an event is not the same thing as what the event LOOKS like: the overlay opens the frame after the Tab press, a weather tint ramps in and a stage change is behind ScreenFade's cut. What it found in its first 60 seconds: `Terrain._build_mesh` emitted its triangles counter-clockwise seen from +Y and Godot's front face is the CLOCKWISE one, so the entire ground was back-face culled from every camera above it — you fell onto a collider you could not see and the arena read as props floating over the backdrop disc — fixed by emitting the winding Godot actually wants and nailed down by `_assert_winding()`, which checks the first triangle on every build and `push_error`s if its right-hand normal points up, so from here on the mistake fails a HEADLESS soak; the guard was shown failing on the old order and silent on the new one before it was kept)
- Iteration 61: Four HUD overlaps nobody could see (all of them geometry, all of them confirmed in a screenshot and re-shot after the fix — see `docs/AUDITORIA-VISUAL.md` for the before/after pairs. The weather chip was placed 26 px below the run timer's TOP inside a label whose font is 34, so for the whole of every weather the clock was cut in half; it now hangs off `_timer_label.size.y + WEATHER_BADGE_GAP`, measured rather than guessed, because the timer's height is its font plus the padding `UiTheme.style_badge` gives it and neither number belongs copied into the line that places the chip. The top-right column is a STACK — kills at 22, points at 56, FPS badge at 88 — and the minimap now hangs below all three: L5-1 of the round-2 audit, deferred there for being visible only when something renders, and the screenshot shows it was worse than the constants suggested, with the points label completely hidden and the FPS badge drawn INSIDE the map; the extra inset also stopped being a solo-only special case, because the stack is anchored to the WINDOW and every split-screen cell along the top edge shares it — in co-op it had none at all and covered the kill counter too. The active power-up row set only two of its four offsets and `set_anchors_preset` zeroes them, so instead of a row 104 px above the HP bar it was a container anchored to the window's bottom edge and 104 px TALL: a column of vertically stretched tiles drawn across the HP bar with the HP numbers split down the middle. And the two loadout strips ended 20 px off the bottom while `%PauseHintLabel` sits 12 px off it with a 20 px box, so the first item a raider picked up drew its tile over 'Esc — Pausa'; both strips move to `BOTTOM_STRIP_MARGIN` 36 together, since the pair reads as one row. The four zones of the HUD and which of them may not overlap which are now written down in `docs/ARQUITECTURA.md` instead of living in four unrelated constants)
